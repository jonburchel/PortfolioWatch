<Window x:Class="PortfolioWatch.Views.TaxStatusEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:emoji="clr-namespace:Emoji.Wpf;assembly=Emoji.Wpf"
        xmlns:converters="clr-namespace:PortfolioWatch.Converters"
        xmlns:views="clr-namespace:PortfolioWatch.Views"
        mc:Ignorable="d"
        Title="Edit Tax Status" SizeToContent="Height" Width="500"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">

    <Window.Resources>
        <converters:PieSliceConverter x:Key="PieSliceConverter"/>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <converters:InverseBooleanToVisibilityConverter x:Key="InverseBoolToVis"/>
        
        <Style x:Key="ValueTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource ControlBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryForegroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ControlBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="HorizontalContentAlignment" Value="Right"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="2"/>
            <Setter Property="Width" Value="60"/>
        </Style>

        <Style x:Key="SliderThumbStyle" TargetType="{x:Type Thumb}">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Height" Value="18"/>
            <Setter Property="Width" Value="18"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Grid>
                            <Ellipse Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type Slider}}}" 
                                     Stroke="{DynamicResource WindowBackgroundBrush}" 
                                     StrokeThickness="2"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SliderRepeatButtonStyle" TargetType="{x:Type RepeatButton}">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="IsTabStop" Value="False"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RepeatButton}">
                        <Rectangle Fill="Transparent"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ColoredSliderStyle" TargetType="{x:Type Slider}">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="IsSnapToTickEnabled" Value="True"/>
            <Setter Property="TickFrequency" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Slider}">
                        <Grid VerticalAlignment="Center">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto" MinHeight="{TemplateBinding MinHeight}"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <!-- Track Background (Unfilled part) -->
                            <Border Grid.Row="1" Height="4" Background="{DynamicResource ControlBorderBrush}" CornerRadius="2" VerticalAlignment="Center"/>
                            
                            <!-- Track -->
                            <Track x:Name="PART_Track" Grid.Row="1">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="{x:Static Slider.DecreaseLarge}">
                                        <RepeatButton.Style>
                                            <Style TargetType="RepeatButton">
                                                <Setter Property="OverridesDefaultStyle" Value="True"/>
                                                <Setter Property="IsTabStop" Value="False"/>
                                                <Setter Property="Focusable" Value="False"/>
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="RepeatButton">
                                                            <Border Height="4" Background="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Slider}}" CornerRadius="2" VerticalAlignment="Center"/>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </RepeatButton.Style>
                                    </RepeatButton>
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="{x:Static Slider.IncreaseLarge}" Style="{StaticResource SliderRepeatButtonStyle}"/>
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource SliderThumbStyle}" VerticalAlignment="Center"/>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="{DynamicResource WindowBackgroundBrush}" CornerRadius="12" Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" ShadowDepth="0" Opacity="0.5"/>
        </Border.Effect>
        <Border Background="Transparent" CornerRadius="12" BorderBrush="{DynamicResource WindowBorderBrush}" BorderThickness="1" ClipToBounds="True">
            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <TextBlock Text="Tax Status and Allocation" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryForegroundBrush}" HorizontalAlignment="Center" Margin="0,0,0,15"/>

                <!-- Unspecified Allocation Display -->
                <Grid Grid.Row="1" Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="160"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Rectangle Width="12" Height="12" Fill="Gray" RadiusX="2" RadiusY="2" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBlock Grid.Column="1" Text="Unspecified" VerticalAlignment="Center" Foreground="{DynamicResource PrimaryForegroundBrush}" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="3" Text="{Binding UnspecifiedPercentage, StringFormat={}{0:N3}%}" VerticalAlignment="Center" Foreground="{DynamicResource PrimaryForegroundBrush}" FontWeight="Bold"/>
                </Grid>

                <!-- Dynamic Allocations List -->
                <ItemsControl Grid.Row="2" ItemsSource="{Binding Allocations}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type views:TaxAllocationViewModel}">
                            <Grid Margin="0,0,0,10" Background="Transparent">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="160"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Color Indicator -->
                                <Rectangle Grid.Column="0" Width="12" Height="12" Fill="{Binding Brush}" RadiusX="2" RadiusY="2" VerticalAlignment="Center" Margin="0,0,10,0"/>

                                <!-- Name (Editable) -->
                                <Grid Grid.Column="1" VerticalAlignment="Center" Margin="0,0,10,0">
                                    <!-- Display Mode -->
                                    <TextBlock Text="{Binding Name}" 
                                               Foreground="{DynamicResource PrimaryForegroundBrush}"
                                               Visibility="{Binding IsEditingName, Converter={StaticResource InverseBoolToVis}}"
                                               Cursor="Hand"
                                               ToolTip="Click to rename">
                                        <TextBlock.InputBindings>
                                            <MouseBinding MouseAction="LeftClick" Command="{Binding StartEditingCommand}"/>
                                        </TextBlock.InputBindings>
                                    </TextBlock>

                                    <!-- Edit Mode -->
                                    <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                                             Visibility="{Binding IsEditingName, Converter={StaticResource BoolToVis}}"
                                             Background="{DynamicResource ControlBackgroundBrush}"
                                             Foreground="{DynamicResource PrimaryForegroundBrush}"
                                             BorderBrush="{DynamicResource ControlBorderBrush}"
                                             Padding="2,0">
                                        <TextBox.InputBindings>
                                            <KeyBinding Key="Enter" Command="{Binding StopEditingCommand}"/>
                                            <KeyBinding Key="Escape" Command="{Binding StopEditingCommand}"/>
                                        </TextBox.InputBindings>
                                        <d:TextBox.Style>
                                            <Style TargetType="TextBox">
                                                <Style.Triggers>
                                                    <Trigger Property="IsVisible" Value="True">
                                                        <Setter Property="FocusManager.FocusedElement" Value="{Binding RelativeSource={RelativeSource Self}}"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </d:TextBox.Style>
                                    </TextBox>
                                </Grid>

                                <!-- Slider -->
                                <Slider Grid.Column="2" Value="{Binding Percentage}" Minimum="0" Maximum="100" Margin="5,0,10,0" VerticalAlignment="Center"
                                        Style="{StaticResource ColoredSliderStyle}" Foreground="{Binding Brush}"/>

                                <!-- Value Box -->
                                <TextBox Grid.Column="3" Text="{Binding Percentage, StringFormat={}{0:N3}, UpdateSourceTrigger=LostFocus}" 
                                         Style="{StaticResource ValueTextBoxStyle}" PreviewTextInput="NumberValidationTextBox"/>

                                <!-- Delete Button (Hover only) -->
                                <Button Grid.Column="4" Command="{Binding RemoveCommand}"
                                        Background="Transparent" BorderThickness="0" Margin="5,0,0,0"
                                        Padding="5" Cursor="Hand"
                                        ToolTip="Remove Category">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Visibility" Value="Hidden"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <TextBlock Text="❌" Foreground="#e74c3c" FontSize="10" VerticalAlignment="Center">
                                                            <TextBlock.Effect>
                                                                <DropShadowEffect Color="#e74c3c" BlurRadius="5" ShadowDepth="0" Opacity="0"/>
                                                            </TextBlock.Effect>
                                                            <TextBlock.Style>
                                                                <Style TargetType="TextBlock">
                                                                    <Style.Triggers>
                                                                        <Trigger Property="IsMouseOver" Value="True">
                                                                            <Setter Property="Effect">
                                                                                <Setter.Value>
                                                                                    <DropShadowEffect Color="#e74c3c" BlurRadius="8" ShadowDepth="0" Opacity="0.8"/>
                                                                                </Setter.Value>
                                                                            </Setter>
                                                                        </Trigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </TextBlock.Style>
                                                        </TextBlock>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=Grid}, Path=IsMouseOver}" Value="True">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Add Button -->
                <Button Grid.Row="3" Command="{Binding AddCategoryCommand}"
                        HorizontalAlignment="Left" Margin="0,10,0,20"
                        Background="Transparent" BorderThickness="0" Cursor="Hand">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="+" Foreground="{DynamicResource SecondaryForegroundBrush}" FontWeight="Bold" Margin="0,0,5,0"/>
                        <TextBlock Text="Add Category" Foreground="{DynamicResource SecondaryForegroundBrush}"/>
                    </StackPanel>
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <ContentPresenter/>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <!-- Footer Buttons -->
                <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Cancel" Click="CancelButton_Click" 
                            Background="Transparent" Foreground="{DynamicResource SecondaryForegroundBrush}" 
                            BorderThickness="0" Margin="0,0,10,0" Padding="10,5"/>
                    <Button Content="Save" Click="SaveButton_Click" 
                            Background="{DynamicResource ControlBackgroundBrush}" Foreground="{DynamicResource PrimaryForegroundBrush}" 
                            BorderBrush="{DynamicResource ControlBorderBrush}" BorderThickness="1" 
                            Padding="15,5">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="4"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </Border>
</Window>
